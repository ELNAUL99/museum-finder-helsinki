# Copy to .env and source it before running the backend, or export these directly.

# AI natural-language search. Set either key - or neither, in which case the app
# still works using the built-in keyword interpreter.
# With both set, Claude is preferred; pin one with AI_PROVIDER=claude|mistral|none.
ANTHROPIC_API_KEY=
MISTRAL_API_KEY=

# Database (defaults match docker-compose.yml)
DB_URL=jdbc:postgresql://localhost:5432/museumfinder
DB_USER=museumfinder
DB_PASSWORD=museumfinder

# Required in any real deployment: openssl rand -base64 48
JWT_SECRET=
