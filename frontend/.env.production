# Where the deployed frontend sends API calls. Not a secret - it is a public URL,
# and keeping it in the repo means the build needs no dashboard configuration.
VITE_API_BASE_URL=https://museum-finder-helsinki-api.onrender.com
